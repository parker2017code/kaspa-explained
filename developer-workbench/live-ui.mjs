import {V6BrowserWallet,v6Call,v6Hash,v6Coins} from '/assets/src/v6-browser-wallet.mjs';
import {createLiveEngine} from '/assets/workbench/live-engine.mjs';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let activeNamespace=localStorage.getItem('kaspa-studio-live:active')||'kaspa-studio-live:';
const namespaceStore=prefix=>({getItem:k=>localStorage.getItem(prefix+k),setItem:(k,v)=>localStorage.setItem(prefix+k,v),removeItem:k=>localStorage.removeItem(prefix+k)});
const store=namespaceStore(activeNamespace);
let owned=false,releaseOwnership=null;
let root=null,wallet=null,engine=null,initializing=null,busy=false,error='',message='',principal='50000000',delay='120000',newAgreement=false;
const button=(id,label,disabled=false,primary=false)=>`<button type="button" data-live="${id}"${disabled||!owned?' disabled':''}${primary?' class="primary"':''}>${label}</button>`;
function download(data,name){const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function status(){return engine?.status()||{stage:'empty',receipts:[]}}
function render(){if(!root?.isConnected)return;const s=status(),ready=wallet?.ready,review=s.review,pending=s.pending;root.innerHTML=`<div class="live-layout"><section class="source-panel"><div class="panel-heading"><div><h2>Execute an escrow on Testnet</h2><div class="panel-caption">A real wallet → contract deposit → payout → saved receipt.</div></div><span class="pill positive">Testnet-10</span></div><div class="live-body"><div class="live-steps" aria-label="Execution progress">${[['empty','1 · Wallet'],['funded','2 · Deposit'],['complete','3 · Payout']].map(([id,label])=>`<div class="live-step ${s.stage===id?'active':''}">${label}</div>`).join('')}</div><p class="lede">This path sends real transactions with free test coins. Buyer, seller, and arbiter are separate keys controlled by this browser, so you can test each role.</p>${error?`<div class="error-banner" role="alert">${esc(error)}</div>`:''}${message?`<p class="live-message" role="status">${esc(message)}</p>`:''}${s.stage==='checking'?'<p class="live-message" role="status">Checking saved acceptance against the node. No transaction will be sent.</p>':''}${!ready?`<div class="live-actions">${button('create',busy?'Creating wallet…':'Create test wallet',busy,true)}${button('restore','Restore recovery file',busy)}</div><p class="field-help">Creating a wallet generates fresh keys locally. It requests no coins and sends no transaction.</p>`:`<div class="live-wallet"><div><div class="metric-label">Buyer available</div><div class="metric-value">${v6Coins(wallet.balances[0])} <span class="metric-unit">tKAS</span></div></div><div><div class="metric-label">Seller available</div><div class="metric-value">${v6Coins(wallet.balances[1])} <span class="metric-unit">tKAS</span></div></div><span class="pill">${esc(wallet.network)}</span></div><details class="live-addresses"><summary>Inspect participant addresses</summary>${wallet.addresses.map((a,i)=>`<p>${['Buyer','Seller','Arbiter'][i]}<code>${esc(a)}</code></p>`).join('')}</details><div class="live-actions">${!wallet.data.faucet?.acceptingBlock?button('faucet',wallet.data.faucet?'Check / retry same coin request':'Get 10 free test coins',busy,true):''}${button('check','Check status',busy)}</div>${wallet.data.faucet&&!wallet.data.faucet.acceptingBlock?'<p class="field-help">The coin request is saved. Checking status does not create another request.</p>':''}${(s.stage==='empty'||s.stage==='complete'&&newAgreement)&&!pending&&!review?`<div class="live-terms"><label class="field"><span class="field-top">Deposit (sompi)</span><input data-live-field="principal" type="number" min="20000000" max="100000000" step="1000000" value="${esc(principal)}"><span class="field-help">${v6Coins(principal)} test coins, plus the displayed network fee.</span></label><label class="field"><span class="field-top">Refund delay (milliseconds)</span><input data-live-field="delay" type="number" min="60000" max="86400000" step="1000" value="${esc(delay)}"><span class="field-help">Measured from the node's current median time. Refunds still require an approved transaction.</span></label></div><div class="live-actions">${button('fund','Review contract deposit',busy||wallet.balances[0]<20000000n,true)}</div>`:''}${s.stage==='funded'&&!review&&!pending?`<div class="explain-evidence">The contract deposit was accepted. Choose an exit and review its exact payout before signing.</div><div class="live-actions">${button('release','Review seller payout',busy,true)}${button('refund','Check refund eligibility',busy)}${button('resolve','Review arbiter payout',busy)}</div>`:''}${s.stage==='complete'?`<div class="explain-evidence">The payout was accepted. This agreement is complete. Its receipts and recovery record are retained below.</div><div class="live-actions">${button('new','Start another agreement',busy)}</div>`:''}${review?`<section class="live-review" aria-label="Transaction approval"><div class="eyebrow">Your approval · one transaction</div><h3>${esc(review.action)} · exact transaction review</h3><p>Network fee: <strong>${v6Coins(review.feeSompi)} tKAS</strong>. Signing happens only in this browser, after this approval.</p><table><thead><tr><th>Output</th><th>Amount</th></tr></thead><tbody>${(review.outputs||[]).map((o,i)=>`<tr><td>${esc(o.label||o.address||`Output ${i+1}`)}</td><td>${v6Coins(o.amount??o.value??o.amountSompi)} tKAS</td></tr>`).join('')}</tbody></table><details><summary>Inspect unsigned transaction</summary><pre>${esc(typeof review.transaction==='string'?review.transaction:JSON.stringify(review.transaction,null,2))}</pre></details><div class="live-actions">${button('confirm','Approve, sign and send',busy,true)}${button('cancel','Cancel review',busy)}</div></section>`:''}${pending?`<div class="live-review"><h3>Signed transaction saved</h3><p>Its outcome is unresolved. Checking status never signs or sends it again. New spending stays blocked.</p><code>${esc(pending.id||pending.transactionId||'')}</code><div class="live-actions">${button('check','Check acceptance',busy)}${['uncertain','submitting','saved'].includes(pending.status)?button('retry','Retry the identical saved transaction',busy):''}</div></div>`:''}<section class="live-history"><h3>Accepted receipts</h3>${s.receipts?.length?`<ol>${s.receipts.map(r=>`<li><strong>${esc(r.action||r.kind||'Transaction')}</strong> <span class="pill">${r.acceptanceVerified?'Accepted':'Checking acceptance'}</span><a href="https://explorer-tn10.kaspa.org/txs/${esc(r.id||r.transactionId)}" target="_blank" rel="noopener noreferrer">${esc(r.id||r.transactionId)}</a>${r.acceptanceVerified&&r.acceptingBlock?`<small>Accepting block ${esc(r.acceptingBlock)}</small>`:''}</li>`).join('')}</ol>`:'<p class="field-help">No accepted contract transactions recorded yet.</p>'}</section>`}</div></section><aside class="inspector"><div class="panel-heading"><h2>Wallet & recovery</h2></div><div class="live-body"><p class="field-help">This is a disposable Testnet wallet. Its keys stay in this browser; the local server receives no private key.</p><label class="field"><span class="field-top">Recovery password</span><input id="live-password" type="password" autocomplete="new-password" minlength="12" placeholder="At least 12 characters"></label><div class="live-actions">${button('backup','Download encrypted recovery',busy||!ready)}</div><input type="file" id="live-recovery-file" accept=".json,application/json" hidden><p class="field-help">Keep the recovery file and its password. The file preserves this wallet and the exact transaction journal. A saved transaction is not automatically replayed.</p><hr><h3>What is live in this beta?</h3><p class="field-help">Escrow funding, release, eligible refund, and arbiter resolution use the real Testnet node. The five application starters have separate compiler and native VM checks. Those checks do not submit transactions.</p></div></aside></div>`;}
async function engineFor(w){await w.connect();const r=await fetch('/assets/public-templates.json');if(!r.ok)throw Error('Contract templates are unavailable.');const t=await r.json();return createLiveEngine({sdk:w.sdk,rpc:w.rpc,wallet:{address:w.address,publicKey:w.owners[0],signInput:(tx,index,signer)=>w.sign(tx,index,{owner:signer.publicKey||signer.signer})},save:async data=>{w.data.studioLive=data;await w.save()},snapshot:w.data.studioLive||null,template:t.templates.escrow});}
async function connect(){await wallet.connect();if(!engine)engine=await engineFor(wallet);return engine;}
async function ownership(){if(!navigator.locks)throw Error('This browser cannot safely lock the test wallet. Use a browser with Web Locks support.');await new Promise((resolve,reject)=>{navigator.locks.request('kaspa-studio-live-wallet',{ifAvailable:true},async lock=>{if(!lock){reject(Error('This test wallet is already open in another tab. Close that tab and reload this page to use it here.'));return;}owned=true;resolve();await new Promise(r=>releaseOwnership=r);}).catch(reject);});}
async function initialize(){if(wallet)return;await ownership();wallet=new V6BrowserWallet({storage:store,onChange:render});if(await wallet.restore()){await connect();await engine.check();await wallet.refresh();}}
async function requestCoins(){await connect();if(!wallet.data.faucet){const{sink}=await v6Call(wallet.rpc.getSink());wallet.data.faucet={address:wallet.address,requestId:crypto.randomUUID(),requestedAt:Date.now(),checkpoint:sink};await wallet.save();}const claim=wallet.data.faucet;if(!claim.acceptingBlock){const r=await fetch('/api/faucet',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({address:claim.address,requestId:claim.requestId}),signal:AbortSignal.timeout(25000)});const data=await r.json();if(!r.ok)throw Error(data.error||'The test coin request could not finish.');if(data.network!=='testnet-10'||String(data.amount)!=='1000000000'||!v6Hash(data.transactionId)||claim.id&&claim.id!==data.transactionId)throw Error('The faucet returned an unexpected receipt. The saved request is retained.');claim.id=data.transactionId;await wallet.save();await wallet.observeFaucet();}await wallet.refresh();message=claim.acceptingBlock?'The node accepted the test-coin transfer. You can review a contract deposit.':'Coin request saved. Check status for acceptance.';}
async function act(action){if(busy||!owned)return;const password=root?.querySelector('#live-password')?.value||'';busy=true;error='';message='';render();try{if(action==='new'){newAgreement=true;}else if(action==='create'){await wallet.create();await connect();await wallet.refresh();message='Fresh test wallet created. Requesting coins is a separate action.';}else if(action==='faucet')await requestCoins();else if(action==='check'){await connect();await wallet.observeFaucet();await engine.check();await wallet.refresh();message='Status checked without signing or resubmitting.';}else if(['fund','release','refund','resolve'].includes(action)){await connect();const terms=action==='fund'?{principalSompi:principal,sellerPublicKey:wallet.owners[1],arbiterPublicKey:wallet.owners[2],refundDelayMs:Number(delay)}:undefined;await engine.prepare({action,...(terms?{terms}:{})});}else if(action==='retry'){await engine.retry(status().pending.transactionId);await engine.check();await wallet.refresh();message='Retried only the saved bytes. No replacement transaction was signed.';}else if(action==='confirm'){await engine.confirm(status().review.id);await engine.check();await wallet.refresh();message=status().pending?'Signed transaction saved and submitted. Check acceptance to observe its outcome.':'The node accepted this transaction.';}else if(action==='cancel'){await engine.cancelReview(status().review.id);message='Review cancelled. No transaction was signed or sent.';}else if(action==='backup'){const envelope=await wallet.backup(password);download({kind:'kaspa-studio-recovery',version:1,wallet:envelope},'kaspa-studio-testnet-recovery.json');message='Encrypted recovery downloaded.';}}catch(e){error=e.safeMessage||e.message||'The operation could not finish. Your saved wallet is retained.';}finally{busy=false;render();root?.querySelector(`[data-live="${action}"]`)?.focus({preventScroll:true});}}
async function restore(file,password){if(!file)return;if(file.size>9000000){error='Recovery file is too large.';render();return}busy=true;error='';render();let candidate;try{const wrapper=JSON.parse(await file.text());if(wrapper.kind!=='kaspa-studio-recovery'||wrapper.version!==1)throw Error('Choose a Kaspa Studio recovery file.');const staging=new Map(),memory={getItem:k=>staging.get(k)||null,setItem:(k,v)=>staging.set(k,v),removeItem:k=>staging.delete(k)};candidate=new V6BrowserWallet({storage:memory});await candidate.importBackup(wrapper.wallet,password);if(wallet?.ready&&wallet.address!==candidate.address)throw Error('This file belongs to another wallet. Your current wallet has been preserved.');const restoredEngine=await engineFor(candidate);await restoredEngine.check();await candidate.refresh();const prefix='kaspa-studio-live:restored:'+crypto.randomUUID()+':';const persistent=namespaceStore(prefix);for(const[k,v]of staging)persistent.setItem(k,v);localStorage.setItem('kaspa-studio-live:active',prefix);activeNamespace=prefix;candidate.storage=persistent;const previous=wallet;wallet=candidate;engine=restoredEngine;candidate=null;wallet.onChange=render;if(previous)await previous.close();message='Wallet restored into a new local record. Earlier records were preserved; nothing was resubmitted.';}catch(e){error=e.message||'Could not decrypt this recovery file.';if(candidate)await candidate.close();}finally{busy=false;render()}}
export async function mountLiveUI(container){root=container;if(!initializing){busy=true;initializing=initialize().catch(e=>{error=e.message;}).finally(()=>{busy=false;render();});}render();await initializing;render();}
document.addEventListener('click',e=>{if(!root?.contains(e.target))return;const b=e.target.closest('[data-live]');if(!b||b.disabled)return;if(b.dataset.live==='restore'){root.querySelector('#live-recovery-file').click();return}void act(b.dataset.live)});
document.addEventListener('input',e=>{if(!root?.contains(e.target))return;if(e.target.dataset.liveField==='principal')principal=e.target.value;if(e.target.dataset.liveField==='delay')delay=e.target.value;});
document.addEventListener('change',e=>{if(e.target.id==='live-recovery-file')void restore(e.target.files[0],root.querySelector('#live-password').value);});
window.addEventListener('pagehide',()=>{releaseOwnership?.();owned=false;void wallet?.close();});

window.addEventListener('pageshow',event=>{if(event.persisted){wallet=null;engine=null;initializing=null;if(root?.isConnected)void mountLiveUI(root);}});
