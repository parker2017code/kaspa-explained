# V6 local acceptance

8 September 2026. The local Chromium tour completed all six chapters with 26 accepted Testnet-10 transactions and five native VM rejection checks. The preserved browser session was resumed across UI corrections. This verifies the integrated local flow; it is not a hosted release approval or a measured first-time-user duration.

[The public receipt record](v6-local-verification.json) contains transaction IDs, accepting blocks, final state, and actual checks. The coordinator inspected the final completion/reload and proof-result screenshots. Pending reload retained the exact transaction, and completion reload restored inventory and scene state without new actions. Existing funded state in `.local/v5-final` was preserved.

The user has since authorized Cloudflare deployment at the unlisted `/covenants/v6` route. The protected cloud host uses a separate fresh signer. CI, hosted durability, full hosted browser acceptance, and canonical-domain verification remain independent gates.

The criteria below remain the acceptance requirements for the cloud replay. Their evidence column describes what must be recorded for that deployed run.

## Required journey outcomes

| Chapter | Action and visible result to verify | Evidence still needed |
| --- | --- | --- |
| Exchange | Compare paying first with the atomic Business transition. Authorize it, observe acceptance, and verify the payment and tool quantity changed together. The modeled tool appears in the cart and remains after reload. Explain that this is a Business ledger quantity, not full KCC20 identity. | Integrated transaction, accepting block, before/after quantities and reload. |
| Workshop | Authorize Pip’s maximum of 2 crops spent and minimum of 1 resource received, with native spending disabled. This proposed barter receives 1 timber; the policy neither requires timber nor restricts the recipient. Execute the allowed trade, separately test a disallowed request, then revoke permission. Verify the precise reported rejection and closed route; do not infer a failed check from the tutorial text. | Allowed receipt, independently observed rejection, revocation result and restored policy state. |
| Trading ring | Settle 3 crops to the toolmaker, 1 tool to the miner and 2 ore to the grower in one combined transaction. All three stores must end with the corresponding quantities, without a replay after refresh. | Combined receipt, three before/after ledgers and persistent visual destinations. |
| Greenhouse | Withdraw a pledge before launch, show that an incomplete group cannot launch, then settle 3 distinct ready V4Launch pledges. Construction follows acceptance. Describe the fixed ready-participant condition rather than an arbitrary funding threshold. | Withdrawal, premature-launch rejection, accepted launch and restored greenhouse state. |
| Habitat | Keep customer payment and courier bond distinct. Exercise recipient-signature release and the eligible chain-age refund path. Show the correct recipients and persistent parcel or returned amounts. Explain that the game supplies the physical witness. | Separate release/refund receipts, eligibility evidence and reconciled payment/bond outputs. |
| Observatory | Run the actual bounded allocation/rate circuit, prover and verifier. Check values in 1–15, public score 42, sum 13, recipient x-only key and nonce. Test an invalid proof separately. Only an observed valid result releases the reward and powers the machine. | Valid and adversarial verification results, public-input binding, accepted reward transition and reload. No privacy guarantee. |

## Final local handoff

Run the tour from its real entry state through a definite ending and free exploration. Check that a newcomer can explain the risk, enforced condition and received result for each chapter. Record the actual duration rather than treating the 12–15 minute target as measured.

Repeat the player-relevant action after a fix that affects it. Check interruption after signing/submission, reload while acceptance is uncertain, exact retry/reconciliation, offline/reconnect, and duplicate event delivery. A timer or panel opening must not complete a chapter, move contract-backed inventory, or invent a block.

Inspect desktop, narrow portrait/landscape, keyboard navigation, touch controls, splitter persistence, 200% text and reduced motion. Exercise Chromium, Firefox and WebKit where available; identify unavailable engines and distinguish emulation from physical-device evidence. Preserve the DAG host across guide updates and verify that observed hashes, parent links and the current accepting block remain inspectable.

Attach the final source/check results and browser evidence to the exact local revision. CI and live publication need separate verification; neither is claimed by this local acceptance record.
